node_modules/
dist/
release/
work/
outputs/
.specstory/
*.log

# Unused local test artifact — not yet wired into the app (see README/OCR
# offline-data discussion); excluded so it doesn't bloat the repo.
eng.traineddata
